#include <stdio.h>

void fonk(int *beginning, int *end)
 {
    int * p;
    for (p = beginning; p <=end; p++)
    printf("%d\n", *p);
    printf("\n");
}
int main()
{
    int arr[7] = {0, 10, 20, 30, 40, 50, 60};
    fonk(arr, &arr[5]);
    fonk(arr, &arr[3]);
    fonk(arr, arr+3);

return 0;
}
